{
  "name": "aquastroke-backend",
  "version": "1.0.0",
  "description": "AQUASTROKE — Developmental Swim Planning Platform",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required — static frontend'",
    "test": "node lib/adapt-engine.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@supabase/supabase-js": "^2.49.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
